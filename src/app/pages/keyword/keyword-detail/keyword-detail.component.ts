import { Component, Input, } from "@angular/core";
import { of } from 'rxjs';

@Component({
    selector: 'keyword-detail',
    styleUrls: ['./keyword-detail.component.scss'],
    templateUrl: './keyword-detail.component.html',
})
export class KeywordDetailComponent {
    
}